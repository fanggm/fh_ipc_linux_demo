2018/5/11(majg)
————————————————————————————————
1、运行指令
   ./mkfs.jffs2 --pad=0x800000 -d app -e 0x10000 -l -o app.jffs2

   其中：
   --pad=0x800000 选项的值和分区大小保持一致;(偏移地址大小)
   -d test 选项表示要制作镜像的目录名;
   -e 0x10000 选项指定flash的擦除大小
   -l 表示小端字节
   -o test.jffs2 指定输出文件名
